import React from 'react';

const TextHeader = ({ children }) => (
  <div className="relative container w-72 mx-auto max-w-text-header text-title-header">
    {children}
  </div>
);

export default TextHeader;
