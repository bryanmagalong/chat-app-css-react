import React from 'react';

const TitleHeader = ({ children }) => (
  <h1 className="text-center lg:text-left title-header text-very-dark-desaturated-violet font-bold">
    {children}
  </h1>
);

export default TitleHeader;
